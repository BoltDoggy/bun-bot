# bun-bot — run_script 专属约定

本文件会被 bun-bot 启动时自动读取并注入系统提示词，请严格遵守。通用项目约定见 AGENTS.md；本文件只描述与 `run_script` 工具相关的行为约定。

## 操作顺序

- **读文件前**：先用 `run_script` 了解目录树（如 `find`/`ls`），确认目标文件存在与位置，再读取。
- **写文件前**：必须先读取目标文件（或确认其不存在），避免盲目覆盖或丢失已有内容。


## 工具

- 代理拥有唯一的工具 `run_script`：编写 JS/TS 脚本，交给 **Bun 实际执行**，观察 stdout/stderr/退出码后再继续推理，直到任务完成。
- 新脚本用 `run_script` 实际运行验证结论，不要凭空猜测；脚本里用 `console.log` 输出结果。

## 技能（.agents/skills）

- 启动时 bun-bot 自动扫描项目根目录 `.agents/skills/`，把技能清单（名称 + 描述 + 文件路径）注入系统提示词。
- 技能两种形态（与 Claude Code 约定一致）：目录型 `.agents/skills/<技能名>/SKILL.md`（可附带 scripts/ 等支持文件）与单文件型 `.agents/skills/<技能名>.md`。
- `SKILL.md` 头部用 YAML frontmatter 声明 `name` 与 `description`；正文是给 agent 的详细指令。
- 当用户任务与某个技能的 description 匹配时，先用 `run_script` 读取该技能文件全文，再按其指令执行；引用技能内支持文件时以技能所在目录为相对基准。
- 用户指令以 `/技能名` 开头（如 `/skill-template`）时，表示要求**执行**该技能：先用 `run_script` 读取对应技能文件全文，再按其 SKILL.md 的指令把实际任务落地；不要只停留在展示技能用途/格式。
- 为什么按需加载：技能清单只占很少 token，全文按需读取，避免一次性塞进系统提示词撑爆上下文。

## 记忆协议（通过 run_script 实现，无需修改任何代码）

- 记忆存储在运行目录下的 `.bunbot/` 子目录中，核心文件为 `.bunbot/memory.md`。
- **任务开始前**：必须先用 `run_script` 读取 `.bunbot/memory.md`（不存在则忽略），把其中的历史记忆纳入当前上下文后再开始推理。这相当于跨会话的"长期记忆"。
- **任务过程中**：遇到值得记录的中间结论（决策原因、踩坑教训、用户偏好），可随时用 `run_script` 追加到 `.bunbot/memory.md`，不必等到结束。
- **任务结束后**：必须用 `run_script` 把本次任务的关键信息追加写入记忆：任务目标、最终结论、关键实现细节、踩过的坑、可复用的经验。使用如下 Markdown 条目格式：

```
### <日期> <会话主题>
- 目标：<一句话>
- 结论：<一句话>
- 经验/教训：<要点，可多条>
- 待办/下一步：<如有>
```

- 记忆防膨胀：追加前先用脚本统计行数；若 `.bunbot/memory.md` 超过 200 行，则保留最近的 30 个条目、丢弃更早的（用脚本重写文件），确保记忆始终精炼可用。
- 禁止把记忆机制写进业务代码；它完全靠本提示词约束代理行为，通过 `run_script` 这个唯一工具落地。
